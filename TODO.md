# TODO

- [ ] Update `physical_health_tracker` view to pass `history_json` into template.
- [ ] Replace `templates/physical_tracker.html` UI with a Health Progress Graph page (Chart.js line chart) using `history_json`.
- [ ] Sanity check: open `/physical/`, submit once, then open `/physical-tracker/` to confirm chart shows and labels render.

